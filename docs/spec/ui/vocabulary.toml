# ADR 0004 R4 — closed vocabulary. Amend the ADR to add a kind.
# Regenerated by scripts/gen_r14.py into Rust + Swift enums.

screen_kinds = ["list", "grid", "detail", "form", "viewer", "settings"]

item_kinds = [
    "text-row",
    "media-item",
    "field-row",
    "toggle-row",
    "action-row",
    "nav-row",
    "progress-row",
    "status-row",
    "chart-row",
]

affordances = [
    "search",
    "filter",
    "sort",
    "selection",
    "primary-action",
    "overflow",
    "banner",
    "confirm",
]

nav_intents = ["push", "sheet", "replace"]

action_roles = ["normal", "destructive"]

status_severities = ["info", "warning", "error"]

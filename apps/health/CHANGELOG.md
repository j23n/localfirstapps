# Changelog

All notable changes are listed here. Headings must match a release tag:
`## [0.2.0]` for tag `v0.2.0`. `scripts/release.sh` writes the heading;
CI refuses a tag that does not have one.

## [Unreleased]

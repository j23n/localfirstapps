# Xcode
DerivedData/
build/
# UniFFI Swift — written by scripts/build_core.sh. A checked-in or
# hand-copied LocalGallery/GalleryCore.swift will not match a newly
# built xcframework (checksum mismatch at launch).
LocalGallery/GalleryCore.swift
*.xcuserdata
*.xcworkspace

# Generated by xcodegen
LocalGallery.xcodeproj

# Rust core (Cargo.lock is committed on purpose — this is an app, not a lib)
core/target/
linux/target/

# Python (scripts/build_model_pack — the script is committed, its venv is not)
__pycache__/
.venv/

# Local Claude Code permissions
.claude/settings.local.json

# macOS
.DS_Store
